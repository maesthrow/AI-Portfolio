# CLAUDE_RU.md

Этот файл содержит руководство для Claude Code (claude.ai/code) при работе с кодом в данном репозитории.

---

## ⚠️ КРИТИЧЕСКИ ВАЖНО: Активные директории сервисов

**ВСЕГДА используйте эти директории:**
- ✅ `frontend-new/` - Активный Next.js фронтенд (киберпанк-тема)
- ✅ `services/content-api-new/` - Активный Content API с версионированными эндпоинтами
- ✅ `services/rag-api/` - Активный RAG & Agent API
- ✅ `infra/compose.apps.yaml` - Активная конфигурация Docker Compose

**НИКОГДА не используйте эти директории (устаревшие):**
- ❌ `frontend/` - Старый фронтенд (устарел)
- ❌ `services/content-api/` - Старый Content API (устарел)
- ❌ `infra/docker-compose.yaml` - Старый Docker Compose (устарел)

Если вы случайно начали работать с устаревшими директориями, **ОСТАНОВИТЕСЬ** и немедленно переключитесь на правильные активные директории.

---

## Обзор проекта

**AI-Portfolio** — это микросервисное приложение-портфолио в стиле киберпанк с возможностями RAG (Retrieval-Augmented Generation). Система состоит из Next.js фронтенда, базы данных PostgreSQL, FastAPI бэкенд-сервисов и векторной базы данных ChromaDB для семантического поиска с агентом на базе LangGraph.

**Технологический стек:**
- Frontend: Next.js 14, React 18, TypeScript, Tailwind CSS, Framer Motion, react-markdown
- Backend: Python 3.12+, FastAPI, SQLAlchemy 2.0, Alembic
- RAG: LangChain, LangGraph, ChromaDB, sentence-transformers
- LLM-инфраструктура: LiteLLM proxy, vLLM (Qwen2.5-7B-Instruct-AWQ), TEI (multilingual-e5-base embeddings)
- База данных: PostgreSQL 16
- Инфраструктура: Docker Compose

---

## Архитектура

Проект следует микросервисной архитектуре с ключевыми сервисами:

### 1. **Content API** (`services/content-api-new/`)
**ВАЖНО: Используйте `content-api-new`, НЕ `content-api` (старая версия)**

- Управляет структурированными данными портфолио с версионированным REST API
- SQLAlchemy ORM с миграциями Alembic
- Версионированный API: все эндпоинты с префиксом `/api/v1/`
- Точка входа: `app/main.py`
- Порт: 8003

Ключевые модули:
- `app/models/` - SQLAlchemy модели:
  - `profile.py` - Profile (full_name, title, subtitle, summary_md)
  - `experience.py` - CompanyExperience (role, company, dates, kind, projects)
  - `experience_project.py` - ExperienceProject (проекты внутри опыта работы)
  - `project.py` - Project (отдельные проекты с slug, technologies, featured flag)
  - `publication.py` - Publication (статьи, посты в блоге)
  - `contact.py` - Contact (email, telegram, github, linkedin)
  - `stats.py` - Stat (ключевые метрики для отображения)
  - `tech_focus.py` - TechFocus (области фокуса технологий)
  - `technology.py` - Technology (элементы технологического стека)
- `app/routers/` - API эндпоинты:
  - `profile.py` - GET `/api/v1/profile`
  - `experience.py` - GET `/api/v1/experience`, GET `/api/v1/experience/{slug}`
  - `stats.py` - GET `/api/v1/stats`
  - `tech_focus.py` - GET `/api/v1/tech-focus`
  - `projects.py` - GET `/api/v1/projects`
  - `publications.py` - GET `/api/v1/publications`
  - `contacts.py` - GET `/api/v1/contacts`
  - `rag.py` - GET `/api/v1/rag/documents` (экспорт данных для индексации RAG)
- `alembic/` - Миграции базы данных

### 2. **RAG API** (`services/rag-api/`)
- Семантический поиск и ответы на вопросы по данным портфолио
- LangGraph агент с памятью (паттерн ReAct)
- Гибридный поиск: плотные эмбеддинги + BM25
- Переранжирование с помощью cross-encoder
- Интерфейс потокового чата
- Точка входа: `app/main.py`
- Порт: 8004

Ключевые модули:
- `app/rag/core.py` - Основной RAG pipeline (`portfolio_rag_answer`)
- `app/rag/retrieval.py` - `HybridRetriever` (dense + BM25)
- `app/rag/rank.py` - Cross-encoder переранжирование
- `app/rag/evidence.py` - Выбор доказательств и упаковка контекста
- `app/agent/graph.py` - LangGraph агент с инструментами
- `app/agent/tools.py` - RAG инструменты для агента
- Эндпоинты агента:
  - POST `/api/v1/agent/chat/stream` - Потоковый чат с агентом

### 3. **Frontend** (`frontend-new/`)
**ВАЖНО: Используйте `frontend-new`, НЕ `frontend` (старая версия)**

- Next.js 14 с App Router
- Серверный рендеринг (SSR)
- UI в стиле киберпанк с анимациями Framer Motion
- react-markdown для рендеринга markdown контента
- Точка входа: `app/page.tsx`
- Порт: 3000

Ключевая структура:
- `app/page.tsx` - Главная страница (загружает все данные через API)
- `app/layout.tsx` - Корневой layout с AgentDock
- `components/` - Модульные компоненты:
  - `agent/AgentDock.tsx` - Глобальный плавающий чат с RAG агентом
  - `agent/AgentChatWindow.tsx` - UI окна чата
  - `agent/AgentInput.tsx` - Ввод сообщений
  - `agent/AgentMessageList.tsx` - Отображение сообщений с потоковой передачей
  - `hero/HeroIntro.tsx` - Секция Hero
  - `about/AboutMeSection.tsx` - Секция "Обо мне" со статистикой
  - `experience/ExperienceSection.tsx` - Таймлайн опыта работы
  - `tech/TechFocusSection.tsx` - Области фокуса технологий
  - `projects/ProjectsSection.tsx` - Избранные проекты
  - `publications/PublicationsSection.tsx` - Статьи/публикации
  - `contacts/ContactsSection.tsx` - Контактная информация
  - `layout/Shell.tsx`, `layout/Footer.tsx` - Компоненты макета
- `lib/api.ts` - Функции API клиента
- `lib/types.ts` - TypeScript определения типов

### 4. **Инфраструктура** (`infra/`)
- Оркестрация Docker Compose (compose.apps.yaml)
- Сервисы:
  - PostgreSQL (внешний, доступ через host.docker.internal)
  - ChromaDB (векторная база данных)
  - vLLM (Qwen2.5-7B-Instruct-AWQ через OpenAI-совместимый API)
  - TEI (Text Embeddings Inference для multilingual-e5-base)
  - LiteLLM (единый прокси для LLM/embeddings)
  - content-api-new (порт 8003)
  - rag-api (порт 8004)

---

## Команды разработки

### Frontend (frontend-new)

```bash
cd frontend-new

npm run dev          # Запуск dev-сервера (порт по умолчанию 3000)
npm run build        # Сборка для продакшена
npm start            # Запуск продакшен-сервера
npm run lint         # Запуск ESLint
```

Переменные окружения (`.env.local`):
```bash
NEXT_PUBLIC_CONTENT_API_BASE=http://localhost:8003/api/v1
NEXT_PUBLIC_AGENT_API_BASE=http://localhost:8004
```

### Content API (content-api-new)

```bash
cd services/content-api-new

# Запуск API (разработка)
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

# Миграции базы данных
alembic revision --autogenerate -m "описание"
alembic upgrade head
alembic downgrade -1

# Проверка статуса миграций
alembic current
alembic history
```

Переменные окружения:
```bash
DATABASE_URL=postgresql+psycopg://user:password@localhost:5433/ai_portfolio_new
FRONTEND_ORIGIN=http://localhost:3000
LOG_LEVEL=INFO
APP_ENV=dev
```

### RAG API

```bash
cd services/rag-api

# Запуск API (разработка)
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

# Индексация документов в ChromaDB (после заполнения content-api)
# Используйте эндпоинт /api/v1/rag/documents из content-api
```

Переменные окружения:
```bash
litellm_base_url=http://localhost:8005/v1
litellm_api_key=dev-secret-123
chat_model=GigaChat
embedding_model=embedding-default
CHROMA_HOST=localhost
CHROMA_PORT=8001
FRONTEND_ORIGIN=http://localhost:3000
LOG_LEVEL=INFO
```

### Docker инфраструктура

```bash
cd infra

# Запуск всех сервисов (рекомендуется)
docker compose -f compose.apps.yaml up -d

# Запуск конкретных сервисов
docker compose -f compose.apps.yaml up -d chroma tei litellm
docker compose -f compose.apps.yaml up -d content-api rag-api

# Проверка здоровья сервисов
docker compose -f compose.apps.yaml ps

# Просмотр логов
docker compose -f compose.apps.yaml logs -f content-api
docker compose -f compose.apps.yaml logs -f rag-api

# Пересборка и перезапуск
docker compose -f compose.apps.yaml up -d --build content-api
```

### Запуск тестов

В настоящее время тестовый набор не настроен. При добавлении тестов используйте:
- Backend: `pytest` (добавить в pyproject.toml)
- Frontend: Jest/React Testing Library (добавить)

---

## Критические правила (из CONTRIBUTING.md)

### Кодировка (СТРОГО)
- **Все файлы ДОЛЖНЫ быть в кодировке UTF-8 без BOM**
- Никогда не используйте Windows-1251, ANSI или битую кириллицу
- Python строки: используйте обычные строки `text = "Корректный русский текст"`
- AI-инструменты ДОЛЖНЫ проверять корректность кодировки перед коммитом

### Миграции базы данных
- **Всегда создавайте миграцию Alembic** при изменении моделей SQLAlchemy
- **Никогда не изменяйте старые миграции** - создавайте новые
- Генерация: `alembic revision --autogenerate -m "сообщение"`
- Расположение: `services/content-api-new/alembic/versions/`

### Соглашения об именовании
- Python: `snake_case` для функций/переменных, `PascalCase` для классов, `snake_case.py` для файлов
- TypeScript/React: `PascalCase.tsx` для компонентов, `useX.ts` для хуков, `camelCase.ts` для утилит

### Изменения кода
- Изменяйте только файлы, явно упомянутые в задаче
- Поддерживайте существующую структуру проекта
- Никаких циклических импортов в бэкенде
- Разделяйте бизнес-логику и контроллеры
- Используйте SQLAlchemy ORM и Pydantic схемы

### Frontend
- Компоненты должны быть детерминированными
- Используйте классы Tailwind CSS в JSX
- Избегайте inline-стилей, кроме анимаций
- Никаких эмодзи, если явно не запрошено

---

## Поток данных

1. **Управление контентом**: Админ/скрипты → PostgreSQL (через content-api-new)
2. **Индексация RAG**: content-api-new `/api/v1/rag/documents` → индексация ChromaDB
3. **Frontend SSR**: Next.js → content-api-new `/api/v1/*` → PostgreSQL → JSON ответ
4. **Чат с агентом**: Пользователь → frontend-new AgentDock → rag-api `/api/v1/agent/chat/stream` → LangGraph агент
5. **Поток RAG запроса**: Вызов инструмента агента → Гибридный поиск (dense + BM25) → Переранжирование → Выбор доказательств → Генерация LLM

---

## Ключевые архитектурные паттерны

### RAG Pipeline
RAG система использует сложный pipeline поиска:
1. **Гибридный поиск**: Комбинирует плотные эмбеддинги (семантические) с BM25 (на основе ключевых слов)
2. **Переранжирование**: Cross-encoder оценивает кандидатов по релевантности
3. **Выбор доказательств**: Выбирает top-k документов с учетом разнообразия
4. **Упаковка контекста**: Вмещает выбранные доказательства в токен-бюджет
5. **Генерация LLM**: Использует упакованный контекст для генерации ответа

См. `services/rag-api/app/rag/core.py:portfolio_rag_answer()`

### Система агента
LangGraph агент (`services/rag-api/app/agent/graph.py`) использует паттерн ReAct:
- Системный промпт требует использования RAG-инструмента для вопросов о портфолио
- Память через `MemorySaver` checkpointer (на основе thread_id)
- Инструменты: `portfolio_rag_tool`, `list_projects_tool`
- Агент ДОЛЖЕН вызывать RAG-инструмент для любых вопросов, связанных с портфолио

### Модели базы данных
Ключевые модели и связи (`services/content-api-new/app/models/`):

**Profile** (`profile.py`):
- Единственный экземпляр с личной информацией (full_name, title, subtitle, location, status, avatar_url, summary_md)

**CompanyExperience** (`experience.py`):
- Опыт работы в компаниях (role, company_name, company_slug, start_date, end_date, is_current)
- Имеет поле `kind`: "commercial" | "personal"
- Содержит markdown поля: `company_summary_md`, `company_role_md`, `description_md`
- Связь один-ко-многим с `ExperienceProject`

**ExperienceProject** (`experience_project.py`):
- Проекты внутри конкретного опыта работы в компании
- Поля: name, slug, period, description_md, achievements_md
- Связь многие-к-одному с CompanyExperience (CASCADE delete)

**Project** (`project.py`):
- Отдельные избранные проекты (не привязаны к опыту)
- Имеет slug, featured флаг, repo_url, demo_url
- Связь многие-ко-многим с Technology через junction-таблицу `project_technology`
- Содержит `long_description_md` и `description_md`

**Technology** (`technology.py`):
- Элементы технологического стека (name, slug, category, order_index)
- Связь многие-ко-многим с Project

**Publication** (`publication.py`):
- Статьи, посты в блоге (title, year, source, url, badge)

**Contact** (`contact.py`):
- Методы контакта (kind: email/telegram/github/linkedin, label, value, url)

**Stat** (`stats.py`):
- Ключевые метрики для отображения (key, label, value, hint, group_name)

**TechFocus** (`tech_focus.py`):
- Группировка областей фокуса технологий

---

## Переменные окружения

Ключевые переменные (см. `infra/.env.dev`):

**База данных:**
- `POSTGRES_DB` - Имя базы данных (например, `ai_portfolio_new`)
- `POSTGRES_USER` - Пользователь базы данных
- `POSTGRES_PASSWORD` - Пароль базы данных
- `POSTGRES_PORT` - Порт PostgreSQL (по умолчанию: 5433)
- `DATABASE_URL` - Полная строка подключения (например, `postgresql+psycopg://user:pass@host:5433/db`)

**Frontend:**
- `FRONTEND_ORIGIN` - Разрешенный CORS origin (например, `http://localhost:3001`)
- `FRONTEND_LOCAL_IP` - Дополнительный CORS origin (например, `http://192.168.1.36:3001`)
- `NEXT_PUBLIC_CONTENT_API_BASE` - Base URL Content API (env фронтенда)
- `NEXT_PUBLIC_AGENT_API_BASE` - Base URL Agent API (env фронтенда)

**LLM-инфраструктура:**
- `LITELLM_BASE_URL` - URL прокси LiteLLM (например, `http://litellm:4000/v1`)
- `LITELLM_MASTER_KEY` - Ключ аутентификации LiteLLM
- `CHAT_MODEL` - Алиас модели чата (например, `GigaChat`, сопоставляется в litellm/config.yaml)
- `EMBEDDING_MODEL` - Алиас модели эмбеддингов (например, `embedding-default`)
- `GIGA_AUTH_DATA` - GigaChat base64 credentials (если используется GigaChat)
- `HF_TOKEN` - HuggingFace токен для загрузки моделей

**Векторная база данных:**
- `CHROMA_HOST` - Хост ChromaDB
- `CHROMA_PORT` - Порт ChromaDB (по умолчанию: 8001 внешний, 8000 внутренний)

**Порты сервисов:**
- `CHROMA_PORT` - 8001 (ChromaDB)
- `VLLM_PORT` - 8002 (vLLM inference)
- `CONTENT_PORT` - 8003 (content-api-new)
- `RAG_PORT` - 8004 (rag-api)
- `LITELLM_PORT` - 8005 (LiteLLM proxy)
- `TEI_PORT` - 8006 (Text Embeddings Inference)

---

## Распространенные ошибки

1. **Неправильные директории сервисов**:
   - ✅ Используйте `content-api-new` и `frontend-new`
   - ❌ НЕ используйте `content-api` или `frontend` (старые версии)

2. **Версионирование API**:
   - Эндпоинты content-api-new имеют префикс `/api/v1/`
   - Frontend должен использовать правильный base URL с префиксом версии

3. **Циклические импорты**: Держите `deps.py` для общих зависимостей, избегайте импортов между API роутерами

4. **Конфликты миграций**:
   - Всегда проверяйте `alembic current` перед созданием миграций
   - Расположение миграций: `services/content-api-new/alembic/versions/`

5. **Проблемы с кодировкой**: Проверяйте кодировку UTF-8, особенно при работе с кириллицей (ОБЯЗАТЕЛЬНО из CONTRIBUTING.md)

6. **Использование инструментов агентом**: RAG агент ДОЛЖЕН вызывать `portfolio_rag_tool` для вопросов о портфолио - не позволяйте LLM отвечать напрямую

7. **Конфигурация CORS**:
   - Убедитесь, что `FRONTEND_ORIGIN` совпадает с URL фронтенда
   - content-api-new строго проверяет CORS

8. **Docker сеть**:
   - PostgreSQL доступен через `host.docker.internal` (внешняя база данных)
   - Внутреннее общение сервисов использует имена сервисов (например, `chroma:8000`, `litellm:4000`)

9. **Алиасы моделей LiteLLM**:
   - Имена моделей должны совпадать с алиасами в `infra/litellm/config.yaml`
   - По умолчанию: `CHAT_MODEL=GigaChat`, `EMBEDDING_MODEL=embedding-default`

10. **Markdown поля**:
    - Многие поля поддерживают markdown (например, `summary_md`, `description_md`, `achievements_md`)
    - Frontend рендерит с помощью `react-markdown`

---

## Справочник структуры файлов

```
AI-Portfolio/
├── frontend-new/                    # ✅ АКТИВНЫЙ Next.js фронтенд (киберпанк-тема)
│   ├── app/
│   │   ├── page.tsx                # Главная страница
│   │   ├── layout.tsx              # Корневой layout с AgentDock
│   │   └── experience/[company_slug]/ # Страница деталей опыта
│   ├── components/
│   │   ├── agent/                  # Компоненты RAG агента
│   │   ├── hero/                   # Hero секция
│   │   ├── about/                  # Секция "Обо мне" со статистикой
│   │   ├── experience/             # Таймлайн опыта
│   │   ├── tech/                   # Области фокуса технологий
│   │   ├── projects/               # Витрина проектов
│   │   ├── publications/           # Список публикаций
│   │   ├── contacts/               # Карточки контактов
│   │   └── layout/                 # Shell, Footer
│   ├── lib/
│   │   ├── api.ts                  # API клиент (SSR)
│   │   └── types.ts                # TypeScript типы
│   ├── package.json
│   └── .env.local                  # Переменные окружения
│
├── frontend/                        # ❌ СТАРАЯ ВЕРСИЯ (устарела)
│
├── services/
│   ├── content-api-new/            # ✅ АКТИВНЫЙ Content API (версионированный API)
│   │   ├── app/
│   │   │   ├── main.py            # Точка входа FastAPI
│   │   │   ├── models/            # SQLAlchemy модели
│   │   │   │   ├── profile.py
│   │   │   │   ├── experience.py
│   │   │   │   ├── experience_project.py
│   │   │   │   ├── project.py
│   │   │   │   ├── publication.py
│   │   │   │   ├── contact.py
│   │   │   │   ├── stats.py
│   │   │   │   ├── tech_focus.py
│   │   │   │   └── technology.py
│   │   │   ├── routers/           # API эндпоинты (/api/v1/*)
│   │   │   │   ├── profile.py
│   │   │   │   ├── experience.py
│   │   │   │   ├── projects.py
│   │   │   │   ├── publications.py
│   │   │   │   ├── contacts.py
│   │   │   │   ├── stats.py
│   │   │   │   ├── tech_focus.py
│   │   │   │   └── rag.py         # Экспорт RAG документов
│   │   │   ├── schemas/           # Pydantic схемы
│   │   │   ├── core/config.py     # Настройки
│   │   │   └── db.py              # Настройка базы данных
│   │   ├── alembic/               # Миграции базы данных
│   │   │   └── versions/
│   │   ├── pyproject.toml
│   │   └── Dockerfile
│   │
│   ├── content-api/                # ❌ СТАРАЯ ВЕРСИЯ (устарела)
│   │
│   └── rag-api/                    # ✅ АКТИВНЫЙ RAG & Agent API
│       └── app/
│           ├── main.py             # Точка входа FastAPI
│           ├── rag/                # Модули RAG pipeline
│           │   ├── core.py         # Основная RAG функция
│           │   ├── retrieval.py    # Гибридный retriever
│           │   ├── rank.py         # Переранжирование
│           │   ├── evidence.py     # Выбор доказательств
│           │   └── prompting.py    # Шаблоны промптов
│           ├── agent/              # LangGraph агент
│           │   ├── graph.py        # Определение графа агента
│           │   └── tools.py        # Инструменты агента
│           └── deps.py             # Общие зависимости
│
├── scripts/
│   ├── ingest.py                   # Индексация RAG документов (legacy)
│   └── settings.py
│
├── infra/
│   ├── compose.apps.yaml           # ✅ Основной docker compose (АКТИВНЫЙ)
│   ├── docker-compose.yaml         # ❌ Старый compose (устарел)
│   ├── .env.dev                    # Шаблон переменных окружения
│   ├── litellm/
│   │   └── config.yaml             # Алиасы моделей LiteLLM
│   └── models/
│       └── intfloat/multilingual-e5-base/  # Модель эмбеддингов TEI
│
├── CONTRIBUTING.md                 # ⚠️ ОБЯЗАТЕЛЬНЫЕ правила для AI-инструментов
└── CLAUDE.md                       # Этот файл (англ. версия)
```

**Ключевые моменты:**
- ✅ **Активные сервисы**: `frontend-new`, `content-api-new`, `rag-api`
- ❌ **Устаревшие**: `frontend`, `content-api` (старые версии, не использовать)
- 🐳 **Docker**: Используйте `infra/compose.apps.yaml` для оркестрации
- 📝 **Правила**: Всегда читайте `CONTRIBUTING.md` перед внесением изменений

---

## При внесении изменений

**Всегда:**
1. **Проверяйте директории сервисов**: Используйте `content-api-new` и `frontend-new`, НЕ старые версии
2. Читайте `CONTRIBUTING.md` сначала (обязательные правила кодировки UTF-8)
3. Проверяйте, что кодировка UTF-8 (особенно для кириллицы в markdown полях)
4. Создавайте миграцию Alembic при изменении SQLAlchemy моделей в `content-api-new`
5. Тестируйте локально перед коммитом
6. Следуйте существующим паттернам кода и соглашениям об именовании
7. Убедитесь, что API эндпоинты включают префикс `/api/v1/` для content-api-new
8. Используйте markdown поля (`*_md`) для богатого контента, который будет рендериться с `react-markdown`

**Никогда:**
1. Не используйте директории `content-api` или `frontend` (старые версии)
2. Не меняйте кодировку файлов с UTF-8
3. Не изменяйте старые миграции Alembic
4. Не создавайте циклические импорты
5. Не смешивайте бизнес-логику с API контроллерами
6. Не меняйте структуру проекта без явного разрешения
7. Не пропускайте версионирование API (префикс `/api/v1/`)
8. Не хардкодьте API URL (используйте переменные окружения)

**Перед коммитом:**
1. ✅ Убедитесь, что изменили правильный сервис (версии `*-new`)
2. ✅ Проверьте отсутствие битых кириллических символов (`????` или `\u041f`)
3. ✅ Запустите миграцию Alembic, если модели изменились
4. ✅ Протестируйте API эндпоинты с правильным префиксом `/api/v1/`
5. ✅ Проверьте настройки CORS, если фронтенд не может достучаться до бэкенда
