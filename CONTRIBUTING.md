# 📄 **CONTRIBUTING.md**

# 🧭 Правила разработки для проекта **AI-Portfolio**

Этот документ обязателен для исполнения **всеми AI-инструментами**:

- GPT-Codex (High/Mini)
- ChatGPT (Coder mode)
- Cursor Codex
- GitHub Copilot
- JetBrains AI
- Claude Code

Документ также обязателен для всех разработчиков, работающих с проектом.

---

# 1. 🎯 Общие правила проекта

1. Проект использует единый стиль, структуру и правила.
2. Любые изменения должны быть:
   - согласованными,
   - атомарными,
   - чистыми,
   - не нарушающими логику БД, API и фронтенда.
3. AI-инструментам запрещено переписывать файлы, не указанные в задаче.

---

# 2. 🔐 Кодировка и работа с кириллицей (СТРОГО)

## 2.1. Все файлы — **UTF‑8 без BOM**

Под запретом:
- ❌ Windows-1251, ANSI
- ❌ «битая» кириллица: `????`, `����`
- ❌ Unicode-последовательности в тексте (`\u041f...`)

## 2.2. Python-файлы
Использовать только обычные строки:

```python
text = "Корректный русский текст"
```

## 2.3. Markdown
Markdown-файлы — чистый UTF‑8.

## 2.4. Обнаружение некорректной кодировки
AI обязан:
1. отменить изменения,
2. пересоздать корректно.

---

# 3. 📁 Структура репозитория
AI запрещено менять структуру без прямого указания.

---

# 4. 📐 Именование

### Python
- функции: `snake_case`
- классы: `PascalCase`
- файлы: `snake_case.py`

### TypeScript/React
- компоненты: `PascalCase.tsx`
- хуки: `useX.ts`
- утилиты: `camelCase.ts`

---

# 5. 🗃 Миграции Alembic

1. Изменил модель → создай миграцию.
2. Запрещено изменять старые миграции.
3. Команда для генерации:

```bash
alembic revision --autogenerate -m "message"
```

---

# 6. 🎨 Фронтенд

Компоненты:
- детерминированные,
- Tailwind внутри JSX,
- без inline-стилей, кроме анимаций.

---

# 7. ⚙ Backend API

Запрещено:
- circular imports
- смешивание бизнес-логики и контроллеров

Разрешено:
- SQLAlchemy ORM
- Pydantic схемы

---

# 8. 🧪 Тесты

Запрещено редактировать или удалять тесты без прямого указания.

---

# 9. 🤖 Правила для AI-инструментов

AI обязан:
- учитывать этот файл автоматически,
- не менять кодировку файлов,
- проверять корректность кириллицы,
- генерировать только чистые diff,
- изменять только те файлы, которые указаны.

AI не имеет права:
- изменять кодировку,
- переписывать весь файл без причины,
- устраивать самовольные «улучшения».

---

# 10. 🧩 Автопроверка AI

Перед выдачей результата AI обязан проверить:
- текст не содержит битой кириллицы,
- diff затрагивает только нужные файлы,
- структура проекта не нарушена,
- миграции корректны,
- TS/Next.js код компилируется.

---

# 11. 🔍 Как использовать CONTRIBUTING.md

AI должен:
1. автоматически читать этот файл,
2. считать его обязательным,
3. применять правила всегда,
4. не требовать повторного присылания файла.

---

# ✔ Конец

Этот документ — главный источник истины.  
Любое нарушение = ошибка.
